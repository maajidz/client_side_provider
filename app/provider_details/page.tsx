import { UserDetailsForm } from "@/components/forms/user-details-form";
import React from "react";

const page = () => {
  return (
    <div>
      <UserDetailsForm initialData={null} />
    </div>
  );
};

export default page;
